var __rest = (this && this.__rest) || function (s, e) {
    var t = {};
    for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p) && e.indexOf(p) < 0)
        t[p] = s[p];
    if (s != null && typeof Object.getOwnPropertySymbols === "function")
        for (var i = 0, p = Object.getOwnPropertySymbols(s); i < p.length; i++) {
            if (e.indexOf(p[i]) < 0 && Object.prototype.propertyIsEnumerable.call(s, p[i]))
                t[p[i]] = s[p[i]];
        }
    return t;
};
import React from 'react';
import { StyleSheet } from 'react-native';
import { Interaction } from '../../theme';
import { Select as RNSelect, } from './select.component.tsx';
import { WebEventResponder, } from '../support/services';
export class Select extends React.Component {
    constructor() {
        super(...arguments);
        this.selectRef = React.createRef();
        this.webEventResponder = WebEventResponder.create(this);
        this.onMouseEnter = () => {
            if (!this.isVisible) {
                this.selectRef.current.props.dispatch([Interaction.HOVER]);
            }
            if (this.props.onMouseEnter) {
                this.props.onMouseEnter();
            }
        };
        this.onMouseLeave = () => {
            if (!this.isVisible) {
                this.selectRef.current.props.dispatch([]);
            }
            if (this.props.onMouseLeave) {
                this.props.onMouseLeave();
            }
        };
        this.onFocus = () => {
            this.selectRef.current.props.dispatch([Interaction.FOCUSED]);
            if (this.props.onFocus) {
                this.props.onFocus();
            }
        };
        this.onBlur = () => {
            this.selectRef.current.props.dispatch([]);
            if (this.props.onBlur) {
                this.props.onBlur();
            }
        };
    }
    get isVisible() {
        return this.selectRef.current.state.visible;
    }
    render() {
        const _a = this.props, { controlStyle } = _a, restProps = __rest(_a, ["controlStyle"]);
        return (<RNSelect {...restProps} {...this.webEventResponder.eventHandlers} ref={this.selectRef} controlStyle={[controlStyle, styles.element]}/>);
    }
}
const styles = StyleSheet.create({
    element: {
        // @ts-ignore
        outlineWidth: 0,
    },
});
//# sourceMappingURL=select.component.web.js.map